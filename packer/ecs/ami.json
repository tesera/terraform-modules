{
  "variables": {
    "profile": "",
    "region": "",
    "ami_regions": ""
  },
  "builders": [
    {
      "type": "amazon-ebs",
      "profile": "{{user `profile`}}",
      "region": "{{user `region`}}",
      "ami_regions": "{{user `ami_regions`}}",
      "source_ami_filter": {
        "filters": {
          "virtualization-type": "hvm",
          "name": "amzn-ami-*-amazon-ecs-optimized",
          "root-device-type": "ebs"
        },
        "owners": [
          "591542846629"
        ],
        "most_recent": true
      },
      "instance_type": "t2.micro",
      "ssh_username": "ec2-user",
      "ami_name": "amzn-ami-{{isotime \"20060102030405\"}}-amazon-ecs-optimized"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "script": "user_data.sh",
      "execute_command": "sudo -S env {{ .Vars }} {{ .Path }}"
    }
  ]
}